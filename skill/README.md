# Figma Design Pipeline Skill

Bundled copy of the `figma-design-pipeline` skill for package-based installers.

This directory exists so the npm package can install the skill without depending on the monorepo layout.
